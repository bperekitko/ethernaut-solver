import { FunctionComponent } from 'react';
import React from 'react';

const Fallback: FunctionComponent<unknown> = () => {
  return <div>Here will be the Fallback level content</div>;
};

export default Fallback;
