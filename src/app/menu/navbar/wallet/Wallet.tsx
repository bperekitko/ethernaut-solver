import React, { FunctionComponent } from 'react';
import styles from './wallet.module.scss';

const Wallet: FunctionComponent<unknown> = () => {
  return <button className={styles.wallet}>Here will be Wallet button</button>;
};

export default Wallet;
