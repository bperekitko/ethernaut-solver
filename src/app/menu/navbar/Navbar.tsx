import React, { FunctionComponent } from 'react';
import styles from './navbar.module.scss';

const Navbar: FunctionComponent<unknown> = () => {
  return <div className={styles.navbar}>Here will be navbar</div>;
};

export default Navbar;
